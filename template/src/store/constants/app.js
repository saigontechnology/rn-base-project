import {actionTypes} from '../actionTypes'

const GET_SETTING_APP_ACTIONS = actionTypes('GET_SETTING_APP_ACTIONS')

const LOGIN_ACTIONS = actionTypes('LOGIN_ACTIONS')

export const APP_CONSTANTS_ACTIONS = {
  GET_SETTING_APP_ACTIONS,
  LOGIN_ACTIONS,
}
